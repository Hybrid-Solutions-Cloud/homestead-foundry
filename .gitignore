# Node / tooling
node_modules/
dist/
*.log

# Secrets and local env (never commit)
.env
.env.*
.dev.vars
*.key
*.pem
*.pfx
*.p12
secrets.json

# OS / editor
.DS_Store
Thumbs.db
.idea/

# Scratch
tmp/
scratch/
.venv/
__pycache__/

# Local/real-value overlays (never committed; placeholders live in versioned examples)
local/
*.local.bicepparam
*.local.json
# Compiled Bicep ARM output (build artifact, regenerate with az bicep build)
infra/*.json
!infra/*.parameters.json

docs/.vitepress/cache
docs/.vitepress/dist
docs/node_modules
pmo/

# Local MCP server config (private endpoints); copy .mcp.json.example and edit
.mcp.json

# Generated per-subscription model catalog (regenerate with scripts/build-model-catalog.mjs)
infra/params/model-catalog.json
