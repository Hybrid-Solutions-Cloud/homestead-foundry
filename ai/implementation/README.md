# implementation/

The deployment runbook and the record of what was actually built. Authored by `foundry-implementer` (Fable).

- `implementation-guide.md` - high-level, ordered, gated runbook derived from the design docs.
- `as-built.md` - what was actually created: real resource names, region, deployments, roles, vault secret names (names only), and budget configuration. Written after deployment.
